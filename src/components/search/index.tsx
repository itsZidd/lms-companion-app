"use client"

import React from "react"

type Props = {
    className?: string
    inputStyle?: string
    placeholder?: string
    iconStyle?: string
}

const Search = ({ className, inputStyle, placeholder, iconStyle }: Props) => {
    return <div>Search</div>
}

export default Search
